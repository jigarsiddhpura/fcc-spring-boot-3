package dev.jigar.runnerz.run;

public enum Location { // enums r used to repr a fixed set of values that a var can take
    // special type of class, repr a grp of constants (unchangeable variables, like final variables)
    INDOOR, OUTDOOR
}
